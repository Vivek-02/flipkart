# Demo for uploading multipart file

We can include thymeleaf dependency and provide a view for the uploaded files.
For now we are sticking with the rest controller annotation and returning a string 
as response.